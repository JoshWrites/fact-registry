# Data

This directory contains anonymized sample data for reproducibility.

Company-specific data is excluded via `.gitignore`. Full datasets live in the private project repository.

Sample files use generic technical documentation examples (not tied to any product).
